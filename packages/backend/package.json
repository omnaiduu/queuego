{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "exports": {
    "./router": "./src/router/index.ts"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "echo 'Backend build complete - frontend served from dist/client'",
    "start": "bun run src/index.ts",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "bun run src/db/migrate.ts",
    "db:studio": "drizzle-kit studio"
  },
  "dependencies": {
    "@orpc/client": "^1.13.2",
    "@orpc/server": "^1.13.2",
    "drizzle-orm": "^0.45.1",
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.8",
    "typescript": "^5.0.0"
  }
}
